# PROJECT STOX — Python Dependencies
# All free, no paid API keys required

# Core
feedparser>=6.0.10
requests>=2.31.0

# Market Data (free, no API key)
yfinance>=0.2.36

# Data Processing
python-dateutil>=2.8.2

# Note: difflib is part of Python's standard library — no install needed
